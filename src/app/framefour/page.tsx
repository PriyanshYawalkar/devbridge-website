import React from "react";
import { Metadata } from "next";
import Page from ".";

export const metadata: Metadata = {
    title: "Join our team - connect with us | DevBridge Community",
    description:
        "Intrested in joining our team? Connect with us to learn more about our open positions and how you can make a difference. submit your application today",
};

export default function FrameFourPage() {
    return <Page />;
}